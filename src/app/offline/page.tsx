export default function OfflinePage() {
  return (
    <main className="page-shell grid min-h-screen place-items-center">
      <section className="section-hero">
        <p className="eyebrow">Offline</p>
        <h1>You can keep reading saved SMILE NGO pages.</h1>
        <p>
          Your connection is unavailable. Reopen the app when internet returns to submit donations,
          messages or volunteer forms.
        </p>
      </section>
    </main>
  );
}
